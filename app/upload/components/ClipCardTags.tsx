import React from "react";
// import { Clip } from "@/types/clip";

// type props = {
//   clip: Clip;
// };

const ClipCardTags = ({  }) => {
  return (
    <div className="flex flex-wrap gap-2">
      {/* {clip.tags.map((tag: string, index: number) => (
        <span
          key={index}
          className="px-3 py-1.5 bg-background border border-slate-700/50 text-slate-300 rounded-lg text-xs font-medium hover:bg-slate-700/60 transition-all duration-200 hover:scale-105"
        >
          #{tag}
        </span>
      ))} */}
    </div>
  );
};

export default ClipCardTags;
